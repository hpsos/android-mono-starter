
/*
 * This file was generated by the Gradle 'init' task.
 *
 * This is a general purpose Gradle build.
 * To learn more about Gradle by exploring our Samples at https://docs.gradle.org/8.5/samples
 */

plugins {
  id ('java-gradle-plugin')
  id ('to.wetransform.semantic-release-version') version '2.1.1'
}

ext {
  // ... other properties
  githubUser = 'tianyingchun'
  githubToken = 'YOUR_GITHUB_TOKEN'
  // (Replace with your actual values)
}


def javaProjects = [
    project(":lib_core"),
    project(":lib_utils"),
    project(":app"),
]


configure(javaProjects) {
    println "${project.name}"
    version '1.0.0'
    apply plugin: 'java-library'

    repositories {
        mavenCentral()
    }
    dependencies {

    }

    test {
        useJUnitPlatform()
    }
    // Configure the "publish" task (in your module's `build.gradle`)
    // publishing {
    //     repositories {
    //         maven {
    //             url "https://maven.pkg.github.com/hpsos/android-mono-starter"
    //             credentials {
    //                 username githubUser
    //                 password githubToken
    //             }
    //         }
    //     }
    // }
    // In your module's `build.gradle` file
    task publishToGithub(type: PublishToMavenLocal) {
        // If necessary, set your pom configuration:
        // pom {
        // ... your project's pom configuration
        // }
    }

    task publish(dependsOn: [publishToGithub, 'publishToMavenLocal']) {
        group = 'Publishing'
    }
    
}